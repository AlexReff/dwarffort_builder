import { h } from "preact";
import { QuickfortLink } from "./quickfort";

export const Footer = () => {
    return (
        <footer id="footer">
            <div class="inner">
                <QuickfortLink />
            </div>
        </footer>
    );
};
